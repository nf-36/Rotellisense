{
  "name": "rotellisense",
  "displayName": "Rotellisense",
  "description": "On-demand Roblox IntelliSense with script decompile tools",
  "version": "0.1.5",
  "repository": {
    "type": "git",
    "url": "https://github.com/nf-36/rotellisense.git"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:lua",
    "onLanguage:luau"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "rotellisense.refreshTypes",
        "title": "Rotellisense: Refresh Live Types",
        "icon": "$(symbol-class)"
      },
      {
        "command": "rotellisense.reconnect",
        "title": "Rotellisense: Reconnect WebSocket"
      },
      {
        "command": "rotellisense.searchScripts",
        "title": "Rotellisense: Search Scripts",
        "icon": "$(search)"
      },
      {
        "command": "rotellisense.refreshScripts",
        "title": "Rotellisense: Refresh Script Results",
        "icon": "$(refresh)"
      },
      {
        "command": "rotellisense.decompileScript",
        "title": "Rotellisense: Decompile Script",
        "icon": "$(file-code)"
      },
      {
        "command": "rotellisense.executeScript",
        "title": "Rotellisense: Execute Script in Roblox",
        "icon": "$(run)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "rotellisense",
          "title": "Rotellisense",
          "icon": "media/rotellisense.svg"
        }
      ]
    },
    "views": {
      "rotellisense": [
        {
          "id": "rotellisense.scriptsView",
          "name": "Scripts",
          "icon": "media/rotellisense.svg"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "rotellisense.searchScripts",
          "when": "view == rotellisense.scriptsView",
          "group": "navigation@1"
        },
        {
          "command": "rotellisense.refreshScripts",
          "when": "view == rotellisense.scriptsView",
          "group": "navigation@2"
        }
      ],
      "editor/title": [
        {
          "command": "rotellisense.executeScript",
          "when": "editorLangId == lua || editorLangId == luau",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "command": "rotellisense.executeScript",
          "when": "editorLangId == lua || editorLangId == luau",
          "group": "rotellisense@1"
        }
      ],
      "view/item/context": [
        {
          "command": "rotellisense.decompileScript",
          "when": "view == rotellisense.scriptsView && viewItem == rotellisense.scriptItem",
          "group": "inline@1"
        }
      ]
    },
    "configuration": {
      "title": "Rotellisense",
      "properties": {
        "rotellisense.wsPort": {
          "type": "number",
          "default": 9000,
          "description": "WebSocket relay port"
        },
        "rotellisense.completionMode": {
          "type": "string",
          "enum": [
            "hybrid",
            "webhook",
            "off"
          ],
          "default": "hybrid",
          "markdownDescription": "Controls completion behavior. `hybrid` merges Rotellisense live path data with nightrains.robloxlsp completion (Rotellisense items are de-prioritized when LSP completion is enabled), `webhook` always prioritizes Rotellisense completion, `off` disables Rotellisense completion entirely (script search/decompile still work)."
        }
      }
    }
  },
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('out', { recursive: true, force: true })\"",
    "compile": "npm run clean && tsc -p ./",
    "watch": "tsc -watch -p ./",
    "vsix": "npm run compile && npx @vscode/vsce package"
  },
  "dependencies": {
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "@types/ws": "^8.18.1",
    "@vscode/vsce": "^3.7.1",
    "typescript": "^5.0.0"
  }
}
